# Local development only. Copy to .env - .env is git-ignored.
# In AWS these values come from SSM Parameter Store, never from a file in git.

APP_VERSION=1.0.0
GIT_COMMIT_SHA=local
GREETING=Hello from docker-compose
LB_PORT=8080
ENABLE_CHAOS=false
